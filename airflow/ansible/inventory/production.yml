# For Contabo server (update with your server details)
all:
  children:
    production:
      hosts:
        contabo-server:
          ansible_host: YOUR_SERVER_IP_HERE
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
          environment_type: production